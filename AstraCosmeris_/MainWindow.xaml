<Window x:Class="AstraCosmeris_.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="AstraCosmeris" Height="300" Width="300"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        MouseMove="Window_MouseMove"
        MouseLeftButtonUp="Window_MouseLeftButtonUp"
        MouseDoubleClick="Window_MouseDoubleClick"
        Loaded="Window_Loaded">
    <Window.ContextMenu>
        <ContextMenu>
            <MenuItem Header="🤏 Smol Astra" Click="MenuSmol_Click"/>
            <MenuItem Header="💬 Big Chat" Click="MenuBigChat_Click"/>
            <MenuItem Header="🗂️ Dashboard" Click="MenuDashboard_Click"/>
            <MenuItem Header="🍅 Pomodoro" Click="MenuPomodoro_Click"/>
            <MenuItem Header="⚙️ Settings" Click="MenuSettings_Click"/>
            <MenuItem Header="❌ Exit" Click="MenuExit_Click"/>
        </ContextMenu>
    </Window.ContextMenu>

    <Grid RenderTransformOrigin="0.5,0.1">
        <Grid.RenderTransform>
            <RotateTransform x:Name="DragRotation" Angle="0"/>
        </Grid.RenderTransform>
        <Image x:Name="PetImage" Stretch="Uniform" Source="/assets/idle/idle.png"/>
    </Grid>
</Window>