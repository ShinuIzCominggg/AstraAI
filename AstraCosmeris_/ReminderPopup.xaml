<Window x:Class="AstraCosmeris_.ReminderPopup"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reminder" Height="90" Width="250"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False">
    <Border Background="#FFFFEEEE" CornerRadius="15" BorderBrush="#FF4444" BorderThickness="2" Margin="5">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Color="Red" Opacity="0.4" ShadowDepth="2"/>
        </Border.Effect>
        <TextBlock x:Name="MessageText" Text="⏰ DẬY LÀM VIỆC ĐI!!!" 
                   TextWrapping="Wrap" FontSize="14" FontWeight="Bold" Foreground="#880000"
                   HorizontalAlignment="Center" VerticalAlignment="Center" TextAlignment="Center"/>
    </Border>
</Window>