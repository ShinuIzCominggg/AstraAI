<Window x:Class="AstraCosmeris_.DailyListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Daily List" Height="550" Width="400"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">

    <Border Background="#FDF8E2" CornerRadius="20" BorderBrush="#2F3273" BorderThickness="3" Margin="15" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" Opacity="0.3" ShadowDepth="5" Color="#2F3273"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#2F3273" Margin="0,0,0,10" CornerRadius="17,17,0,0">
                <Grid>
                    <TextBlock x:Name="TxtTitle" Text="📅 Ngày --/--/----" Foreground="White" FontSize="18" FontWeight="Heavy" Margin="18,15" VerticalAlignment="Center" FontFamily="Segoe UI Black"/>
                    <Button Content="✖" HorizontalAlignment="Right" Width="45" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="20" FontWeight="Bold" Cursor="Hand" Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Margin="15,0,15,15">
                <StackPanel>

                    <Border Background="White" CornerRadius="12" Padding="15" Margin="0,0,0,15">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="5" Opacity="0.1" ShadowDepth="2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="📝 CÔNG VIỆC (TASKS):" FontWeight="Heavy" Foreground="#FF4C4C" FontSize="15" Margin="0,0,0,10"/>
                            <TextBlock x:Name="TxtTasks" Text="Loading..." FontSize="15" TextWrapping="Wrap" Foreground="#333" LineHeight="24"/>
                        </StackPanel>
                    </Border>

                    <Border Background="White" CornerRadius="12" Padding="15" Margin="0,0,0,10">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="5" Opacity="0.1" ShadowDepth="2"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="🎉 SỰ KIỆN (EVENTS):" FontWeight="Heavy" Foreground="#2F3273" FontSize="15" Margin="0,0,0,10"/>

                            <TextBlock x:Name="TxtNoEvents" Text="Không có sự kiện nào." FontStyle="Italic" Foreground="#888" FontSize="14" Visibility="Collapsed"/>

                            <ItemsControl x:Name="EventList">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#F4F6F9" CornerRadius="10" Padding="10" Margin="0,0,0,10">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="15" Foreground="#2F3273" TextWrapping="Wrap"/>
                                                    <TextBlock FontSize="13" Foreground="#666" Margin="0,3,0,0">
                                                        <Run Text="{Binding Type}"/>
                                                        <Run Text=" - "/>
                                                        <Run Text="{Binding Repeat}"/>
                                                    </TextBlock>
                                                    <TextBlock Text="{Binding Location, StringFormat='📍 {0}'}" FontSize="13" Foreground="#888" FontStyle="Italic">
                                                        <TextBlock.Style>
                                                            <Style TargetType="TextBlock">
                                                                <Setter Property="Visibility" Value="Visible"/>
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding Location}" Value="">
                                                                        <Setter Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </TextBlock.Style>
                                                    </TextBlock>
                                                </StackPanel>

                                                <Button Grid.Column="1" Content="🗑️" FontSize="18" Background="Transparent" BorderThickness="0" Cursor="Hand" VerticalAlignment="Center" Click="BtnDeleteEvent_Click" ToolTip="Xóa sự kiện này"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>