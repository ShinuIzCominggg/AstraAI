<Window x:Class="AstraCosmeris_.ChatInputWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Chat với Astra" SizeToContent="WidthAndHeight"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" WindowStartupLocation="CenterScreen">

    <Border Background="#E6FFFFFF" CornerRadius="20" Margin="15" Padding="10,5,10,5"
            BorderBrush="#D0D0D0" BorderThickness="1"
            MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="3"/>
        </Border.Effect>

        <Grid Width="300">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="InputBox" Grid.Column="0" 
                     Background="Transparent" BorderThickness="0" 
                     FontSize="13" FontFamily="Segoe UI" VerticalAlignment="Center"
                     Padding="5" MinHeight="30" TextWrapping="Wrap" MaxHeight="80"
                     KeyDown="InputBox_KeyDown"/>

            <Button x:Name="BtnSend" Grid.Column="1" Content="➤" 
                    Width="32" Height="32" Margin="5,0,0,0"
                    FontSize="16" Foreground="White" Cursor="Hand"
                    BorderThickness="0" Click="BtnSend_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="16"/>
                    </Style>
                </Button.Resources>
                <Button.Background>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#9d7eff" Offset="0.0"/>
                        <GradientStop Color="#6ecbff" Offset="1.0"/>
                    </LinearGradientBrush>
                </Button.Background>
            </Button>
        </Grid>
    </Border>
</Window>