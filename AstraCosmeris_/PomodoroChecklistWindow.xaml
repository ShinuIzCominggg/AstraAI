<Window x:Class="AstraCosmeris_.PomodoroChecklistWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Checklist" Height="550" Width="450" WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" WindowStartupLocation="CenterScreen">

    <Grid>
        <Image Source="pack://application:,,,/assets/pomodoro/todolist_bg.png" Stretch="Uniform" IsHitTestVisible="False"/>

        <Border Margin="65,74,45,100" Background="Transparent" MouseLeftButtonDown="Border_MouseLeftButtonDown">

            <ListView x:Name="LvTasks" Background="Transparent" BorderThickness="0" ScrollViewer.HorizontalScrollBarVisibility="Disabled" RenderTransformOrigin="0.5,0.5" Margin="-2,87,2,52">
                <ListView.RenderTransform>
                    <TransformGroup>
                        <ScaleTransform/>
                        <SkewTransform AngleX="0.551"/>
                        <RotateTransform Angle="0.366"/>
                        <TranslateTransform X="-1.49"/>
                    </TransformGroup>
                </ListView.RenderTransform>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#E0E0E0" BorderThickness="0,0,0,1" Padding="0,8,0,8" HorizontalAlignment="Stretch">
                            <Grid ContextMenuOpening="Grid_ContextMenuOpening">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="40"/>
                                </Grid.ColumnDefinitions>

                                <Grid Grid.Column="0" Margin="{Binding MarginLevel}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="25"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{Binding OrderText}" FontSize="15" FontWeight="Bold" Foreground="#888" VerticalAlignment="Center"/>

                                    <Grid Grid.Column="1" VerticalAlignment="Center">
                                        <TextBox Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                 FontSize="15" Foreground="{Binding TextColor}" 
                                                 Background="Transparent" BorderThickness="0" 
                                                 TextWrapping="Wrap" MinWidth="150"
                                                 IsReadOnly="True" Cursor="Arrow"
                                                 MouseDoubleClick="TaskTextBox_MouseDoubleClick"
                                                 LostFocus="TaskTextBox_LostFocus"
                                                 Loaded="TaskTextBox_Loaded"/>

                                        <Border Height="1" Background="{Binding TextColor}" VerticalAlignment="Center" HorizontalAlignment="Stretch" IsHitTestVisible="False">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsStrikethrough}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </Grid>
                                </Grid>

                                <CheckBox Grid.Column="1" IsChecked="{Binding IsChecked, Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Right" Cursor="Hand" Click="CheckBox_Click">
                                    <CheckBox.Template>
                                        <ControlTemplate TargetType="CheckBox">
                                            <Border Width="20" Height="20" CornerRadius="10" BorderBrush="#2F3273" BorderThickness="2" Background="{TemplateBinding Background}">
                                                <Path x:Name="CheckMark" Data="M 4 10 L 8 14 L 15 5" Stroke="White" StrokeThickness="2" StrokeEndLineCap="Round" StrokeStartLineCap="Round" StrokeLineJoin="Round" Visibility="Collapsed"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter Property="Background" Value="#2F3273"/>
                                                    <Setter TargetName="CheckMark" Property="Visibility" Value="Visible"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </CheckBox.Template>
                                </CheckBox>

                                <Grid.ContextMenu>
                                    <ContextMenu x:Name="TaskContextMenu"/>
                                </Grid.ContextMenu>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Button Content="✖" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,63,40,0" 
                Background="Transparent" BorderThickness="0" Foreground="#FF6B6B" FontSize="24" FontWeight="Bold" Cursor="Hand" Click="BtnClose_Click" RenderTransformOrigin="0.5,0.5">
            <Button.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="19.229"/>
                    <TranslateTransform/>
                </TransformGroup>
            </Button.RenderTransform>
        </Button>

        <Button Content="+" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="0,0,339,104" 
                FontWeight="Bold" FontSize="24" Foreground="White" Background="#FF6B6B" Width="47" Height="47" Cursor="Hand" Click="BtnAddTask_Click">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="20"/>
                </Style>
            </Button.Resources>
        </Button>

    </Grid>
</Window>