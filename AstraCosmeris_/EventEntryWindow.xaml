<Window x:Class="AstraCosmeris_.EventEntryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Event Entry" Height="480" Width="420"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        WindowStartupLocation="CenterScreen" Topmost="True">

    <Border Background="#FDF8E2" CornerRadius="20" BorderBrush="#2F3273" BorderThickness="3" Margin="10" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.4" ShadowDepth="5" Color="#2F3273"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid.Resources>

                <SolidColorBrush x:Key="PrimaryBlue" Color="#2F3273"/>
                <SolidColorBrush x:Key="LighterBlue" Color="#9FB2D4"/>
                <SolidColorBrush x:Key="TextDark" Color="#111111"/>

                <Style TargetType="TextBox">
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="Padding" Value="12,8"/>
                    <Setter Property="BorderBrush" Value="{StaticResource LighterBlue}"/>
                    <Setter Property="BorderThickness" Value="2"/>
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="TextBox">
                                <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10" SnapsToDevicePixels="True">
                                    <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" Margin="0,2"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="true">
                                        <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                    </Trigger>
                                    <Trigger Property="IsFocused" Value="true">
                                        <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                                        <Setter TargetName="border" Property="BorderThickness" Value="2.5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <ControlTemplate x:Key="ComboBoxToggleButton" TargetType="ToggleButton">
                    <Border x:Name="templateRoot" Background="Transparent" BorderBrush="{StaticResource LighterBlue}" BorderThickness="2" CornerRadius="10" SnapsToDevicePixels="true">
                        <Grid HorizontalAlignment="Right" Width="30" Margin="0,0,5,0">
                            <Path x:Name="arrow" Data="M 0 0 L 4 4 L 8 0 Z" Fill="{StaticResource PrimaryBlue}" HorizontalAlignment="Center" VerticalAlignment="Center" SnapsToDevicePixels="false"/>
                        </Grid>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="true">
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                            <Setter TargetName="arrow" Property="Fill" Value="#555"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter TargetName="templateRoot" Property="BorderBrush" Value="{StaticResource PrimaryBlue}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>

                <Style x:Key="ComboBoxItemCute" TargetType="ComboBoxItem">
                    <Setter Property="Padding" Value="10,6"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBoxItem">
                                <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderThickness="0" CornerRadius="8" Margin="2,1" SnapsToDevicePixels="true">
                                    <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Margin="{TemplateBinding Padding}"/>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#9FB2D4"/>
                                        <Setter Property="Foreground" Value="{StaticResource PrimaryBlue}"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="Bd" Property="Background" Value="#DFE9F5"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>

                <Style TargetType="ComboBox">
                    <Setter Property="FontSize" Value="15"/>
                    <Setter Property="Foreground" Value="{StaticResource TextDark}"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Margin" Value="0,0,0,15"/>
                    <Setter Property="ItemContainerStyle" Value="{StaticResource ComboBoxItemCute}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ComboBox">
                                <Grid x:Name="templateRoot" SnapsToDevicePixels="true">
                                    <ToggleButton x:Name="ToggleButton" Template="{StaticResource ComboBoxToggleButton}" Grid.Column="2" Focusable="false" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press"/>

                                    <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" Content="{TemplateBinding SelectionBoxItem}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Margin="12,3,40,3" IsHitTestVisible="false" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>

                                    <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" Placement="Bottom" Margin="1" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                                        <Border x:Name="dropDownBorder" Background="White" BorderBrush="{StaticResource LighterBlue}" BorderThickness="2" CornerRadius="12" Margin="0,3,0,0" MinWidth="{Binding ActualWidth, ElementName=templateRoot}" MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                            <Border.Effect>
                                                <DropShadowEffect BlurRadius="10" Opacity="0.2" ShadowDepth="3"/>
                                            </Border.Effect>
                                            <ScrollViewer x:Name="DropDownScrollViewer">
                                                <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                                    <Canvas x:Name="canvas" HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                                        <Rectangle x:Name="opaqueRect" Fill="{Binding Background, ElementName=dropDownBorder}" Height="{Binding ActualHeight, ElementName=dropDownBorder}" Width="{Binding ActualWidth, ElementName=dropDownBorder}"/>
                                                    </Canvas>
                                                    <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Margin="2"/>
                                                </Grid>
                                            </ScrollViewer>
                                        </Border>
                                    </Popup>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </Grid.Resources>


            <Border Grid.Row="0" Background="#2F3273" Margin="0,0,0,10" CornerRadius="17,17,0,0">
                <Grid>
                    <TextBlock x:Name="TxtTitle" Text="🎉 Sự kiện ngày: --/--/----" Foreground="White" FontSize="18" FontWeight="Heavy" Margin="18,12" VerticalAlignment="Center" FontFamily="Segoe UI Black"/>
                    <Button Content="✖" HorizontalAlignment="Right" Width="45" Background="Transparent" BorderThickness="0" Foreground="White" FontSize="20" FontWeight="Bold" Cursor="Hand" Click="BtnClose_Click"/>
                </Grid>
            </Border>

            <StackPanel Grid.Row="1" Margin="25,10">
                <TextBlock Text="Tên sự kiện:" FontWeight="Bold" Foreground="#2F3273" Margin="0,0,0,6" FontSize="14"/>
                <TextBox x:Name="TxtEventName" Margin="0,0,0,15"/>

                <TextBlock Text="Loại sự kiện:" FontWeight="Bold" Foreground="#2F3273" Margin="0,0,0,6" FontSize="14"/>
                <ComboBox x:Name="CboType">
                    <ComboBoxItem Content="🎂 Sinh nhật"/>
                    <ComboBoxItem Content="🔥 Deadline"/>
                    <ComboBoxItem Content="💖 Kỉ niệm"/>
                    <ComboBoxItem Content="💼 Cuộc họp"/>
                    <ComboBoxItem Content="Khác" IsSelected="True"/>
                </ComboBox>

                <TextBlock Text="Địa điểm:" FontWeight="Bold" Foreground="#2F3273" Margin="0,0,0,6" FontSize="14"/>
                <TextBox x:Name="TxtLocation" Margin="0,0,0,15"/>

                <TextBlock Text="Lặp lại:" FontWeight="Bold" Foreground="#2F3273" Margin="0,0,0,6" FontSize="14"/>
                <ComboBox x:Name="CboRepeat">
                    <ComboBoxItem Content="Không lặp" IsSelected="True"/>
                    <ComboBoxItem Content="Hàng tuần"/>
                    <ComboBoxItem Content="Hàng tháng"/>
                    <ComboBoxItem Content="Hàng năm"/>
                </ComboBox>
            </StackPanel>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="15,0,20,20">
                <TextBlock x:Name="TxtStatus" Text="" Foreground="#FF4C4C" VerticalAlignment="Center" Margin="0,0,15,0" FontSize="14" FontWeight="Bold"/>
                <Button Content="💾 Lưu Sự Kiện" FontSize="17" FontWeight="Heavy" Background="#2F3273" Foreground="White" Padding="20,10" BorderThickness="0" Cursor="Hand" Click="BtnSave_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="12"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>